 
--  step 1

--  SELECT subject as sujects,
-- round(avg(marks),1) AS Avg_Marks_Subjects
-- From marks
-- GROUP BY subject
-- having avg(marks)=85
-- order by avg(marks) desc;\

-- step 2

-- select subject as subjects,
-- round(avg(marks),) as Avg_Marks_Subjects
-- From marks
-- where not subject = 'english'
-- GROUP by subject 
-- having avg(marks) >=85
-- order by avg(marks) desc;


